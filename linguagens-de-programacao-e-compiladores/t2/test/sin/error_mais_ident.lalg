{ Esqueceu algum dos argumentos para o procedimento e funcao (na chamada deles) }
program oi;

	const c = 10;
	var a, b: integer;
	var w: real;

	procedure meu_proc(v: integer; x: real);
	var contador: integer;
	begin
		contador := contador + v;
		write(contador);
		write(x);
	end;

	function minha_func(v: integer; x: real) : integer;

begin
	read(b);
	minha_func(b w); { erro aqui }
	meu_proc(b w); { erro aqui }
end.
